query GetRefreshToken($token: String!) {
  refreshToken(token: $token) {
    token
    user {
      id
      email
      firstName
      lastName
      roles {
        role
        permissions {
          permission
          resource
        }
      }
    }
    identity {
      provider
    }
  }
}
