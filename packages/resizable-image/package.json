{
  "name": "resizable-image",
  "version": "0.0.0",
  "descripton": "resizable image component for dictybase text editor",
  "main": "src/index.ts",
  "author": "Kevin Tun",
  "license": "BSD-2-Clause",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "lint": "TIMING=1 eslint src/ --ext .ts --ext .tsx -f unix --cache",
    "lint:out": "eslint src/ --ext .ts --ext .tsx -f json -o ../../eslint/resizable-image.json"
  },
  "devDependencies": {
    "@types/react": "17.x",
    "@types/react-dom": "17.x",
    "@vitest/coverage-c8": "0.31.x",
    "eslint-config-custom": "*",
    "tsconfig": "*",
    "vitest": "0.31.x"
  },
  "dependencies": {
    "@material-ui/core": "4.x",
    "@material-ui/icons": "4.x",
    "jotai": "^1.12.0",
    "react": "17.x"
  },
  "typedoc": {
    "entryPoint": "./src/index.ts",
    "displayName": "@dictybase/resizable-image"
  }
}
