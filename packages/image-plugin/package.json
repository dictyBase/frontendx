{
  "name": "@dictybase/image-plugin",
  "version": "0.0.0",
  "description": "Image plugin for dictybase text editor",
  "main": "src/index.ts",
  "author": "Kevin Tun",
  "license": "BSD-2-Clause",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "lint": "TIMING=1 eslint src/ --ext .ts --ext .tsx -f unix --cache",
    "lint:out": "eslint src/ --ext .ts --ext .tsx -f json -o ../../eslint/image-plugin.json"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "tsconfig": "*"
  },
  "dependencies": {
    "dicty-graphql-schema": "https://github.com/dictyBase/dicty-graphql-schema#v1.17.0",
    "@dictybase/flex-layout-plugin": "*",
    "fp-ts": "2.x",
    "@dictybase/resizable-image": "*",
    "ts-pattern": "4.x"
  },
  "typedoc": {
    "entryPoint": "./src/index.ts",
    "displayName": "@dictybase/image-plugin"
  }
}
