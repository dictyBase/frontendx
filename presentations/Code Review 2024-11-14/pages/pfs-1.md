# parseFormattedStringToDomElements Refactor

- Used to properly render Publication titles
- The function parses a string that contains format tags and returns an array of formatted and unformatted DOM elements.

