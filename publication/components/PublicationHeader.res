@react.component
let make = () => {
    open Mui
    <Box mt={Box.Value.int(2)} mb={Box.Value.int(2)}>
      <Typography variant=#h2 align=#center>
        {React.string("dictyBase Literature")}
      </Typography>
    </Box>
}