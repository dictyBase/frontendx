name: Oxlint
on: [pull_request]

jobs:
  oxlint:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v5
      - name: Install
        run: yarn install --ignore-engines
      - name: Run Oxlint
        run: yarn oxlint:ci
